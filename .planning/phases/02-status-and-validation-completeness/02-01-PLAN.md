---
phase: 02-status-and-validation-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/usecase/glbc_uc/deploy_pool.go
  - internal/usecase/glbc_uc/deploy_listener.go
  - internal/usecase/glbc_uc/deploy_pool_test.go
  - internal/usecase/glbc_uc/deploy_listener_headers_test.go
  - internal/repository/mocks.go
  - internal/usecase/mocks.go
  - pkg/utils/mocks.go
autonomous: true
requirements: [STAT-01, STAT-02]

must_haves:
  truths:
    - "After a new pool is created, status.createdPools[*].createdMembers contains member data from the spec — populated immediately without a second reconcile"
    - "CreatedGlobalPoolMember.Id is left empty on the create path (populated on subsequent reconciles via the update path's deployPoolMembers)"
    - "Status is saved BEFORE WaitGlobalLoadBalancerActive on all paths (pool create, listener create, listener update)"
    - "Changing a listener's allowed headers in the GLBC spec triggers a listener update API call on the next reconcile"
    - "Unchanged headers (including nil entity vs empty spec) do not trigger a spurious listener update"
  artifacts:
    - path: "internal/usecase/glbc_uc/deploy_pool.go"
      provides: "Pool member status tracking from spec data on create path"
      contains: "statusUpdatePoolMember"
    - path: "internal/usecase/glbc_uc/deploy_listener.go"
      provides: "Headers comparison in buildListenerUpdateRequest + statusAddListener activation"
      contains: "stringSlicesEqualUnordered"
    - path: "internal/usecase/glbc_uc/deploy_pool_test.go"
      provides: "Unit tests for STAT-01 pool member status tracking"
      exports: ["TestDeployPool_PopulatesCreatedPoolMembers", "TestDeployPool_StatusUpdatedOnCreate"]
    - path: "internal/usecase/glbc_uc/deploy_listener_headers_test.go"
      provides: "Unit tests for STAT-02 headers comparison"
      exports: ["TestBuildListenerUpdateRequest_Headers", "TestBuildListenerUpdateRequest_HeadersNoChange", "TestBuildListenerUpdateRequest_HeadersNilEntityEmptySpec"]
  key_links:
    - from: "deploy_pool.go"
      to: "status.go"
      via: "statusUpdatePoolMember call after pool creation, BEFORE WaitGlobalLoadBalancerActive"
      pattern: "statusUpdatePoolMember.*_pool\\.ID"
    - from: "deploy_listener.go"
      to: "status.go"
      via: "statusAddListener calls on create and update paths, BEFORE WaitGlobalLoadBalancerActive"
      pattern: "statusAddListener"
    - from: "deploy_listener.go"
      to: "status.go"
      via: "stringSlicesEqualUnordered for order-independent header comparison"
      pattern: "stringSlicesEqualUnordered\\(currentHeaders"
---

<objective>
Activate pool member status tracking on pool creation (STAT-01) and implement headers comparison in listener update detection (STAT-02).

Purpose: Complete the two stubbed status/validation gaps left after Phase 1 bug fixes. Pool member data must be recorded in status on first creation without requiring a second reconcile. Header drift between spec and entity must trigger listener updates.

Output: Modified deploy_pool.go and deploy_listener.go with activated status tracking, plus unit tests covering both requirements.
</objective>

<execution_context>
@/home/stackops/.claude/get-shit-done/workflows/execute-plan.md
@/home/stackops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-status-and-validation-completeness/02-CONTEXT.md
@.planning/phases/02-status-and-validation-completeness/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From internal/usecase/glbc_uc/status.go:
```go
func (t *defaultModelDeployTask) statusUpdatePoolMember(ctx context.Context, poolId string, name string, poolMembers []v1alpha1.CreatedGlobalPoolMember) error
func (t *defaultModelDeployTask) statusAddListener(ctx context.Context, listenerId string, port int) error
func stringSlicesEqualUnordered(a, b []string) bool
```

From api/v1alpha1/globalloadbalancerconfig_types.go:
```go
type CreatedGlobalPool struct {
    Id                 string                    `json:"id"`
    Name               string                    `json:"name"`
    CreatedPoolMembers []CreatedGlobalPoolMember `json:"createdPoolMembers,omitempty"`
}

type CreatedGlobalPoolMember struct {
    Id             string         `json:"id"`
    Name           string         `json:"name"`
    CreatedMembers []GlobalMember `json:"createdMembers,omitempty"`
}

type GlobalListener struct {
    // ...
    Headers []string `json:"headers,omitempty"`
}
```

From SDK entity (entityv2.GlobalListener):
```go
type GlobalListener struct {
    // ...
    Headers *string `json:"headers"` // comma-joined string, or nil
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Create test stubs for STAT-01 and STAT-02</name>
  <files>internal/usecase/glbc_uc/deploy_pool_test.go, internal/usecase/glbc_uc/deploy_listener_headers_test.go</files>
  <behavior>
    - TestDeployPool_PopulatesCreatedPoolMembers: deployPool on create path returns CreatedGlobalPool with CreatedPoolMembers populated from spec (not empty). ListGlobalPoolMembers is NOT called.
    - TestDeployPool_StatusUpdatedOnCreate: PatchMutateStatusGlobalLoadBalancerConfig is called during pool creation (statusUpdatePoolMember fires).
    - TestBuildListenerUpdateRequest_Headers: differing headers between entity (*string "x-old") and spec ([]string{"x-new"}) produce a non-nil update request with "headers" in message.
    - TestBuildListenerUpdateRequest_HeadersNoChange: same headers produce nil update or no headers entry in message.
    - TestBuildListenerUpdateRequest_HeadersNilEntityEmptySpec: nil entity headers + empty spec headers do not trigger spurious update.
  </behavior>
  <action>
    Create deploy_pool_test.go with two test functions using the established mock pattern (MockVngCloudRepository, MockK8sRepository from repository package, testify/assert+mock):

    1. TestDeployPool_PopulatesCreatedPoolMembers: Create a defaultModelDeployTask with mocks. Set up poolSpec with one PoolMember containing one GlobalMember (name, address, port, weight, monitorPort, subnetID). Mock CreateGlobalPool to return pool with ID. Mock PatchMutateStatusGlobalLoadBalancerConfig to return nil. Mock WaitGlobalLoadBalancerActive. Call deployPool with empty currentPools (forces create path). Assert CreatedPoolMembers[0].Name, CreatedMembers[0].Address, Port, SubnetID match spec. Assert ListGlobalPoolMembers was NOT called.

    2. TestDeployPool_StatusUpdatedOnCreate: Similar setup, simpler poolSpec. Assert PatchMutateStatusGlobalLoadBalancerConfig was called.

    Create deploy_listener_headers_test.go with three test functions:

    3. TestBuildListenerUpdateRequest_Headers: Build currentListener with Headers=ptr.To("x-old"), listenerSpec with Headers=[]string{"x-new"}. Set all other fields equal (AllowedCidrs, Timeout*, GlobalPoolID). Assert updateOptions != nil and message contains "headers".

    4. TestBuildListenerUpdateRequest_HeadersNoChange: Both sides have "x-keep". Assert no headers in message.

    5. TestBuildListenerUpdateRequest_HeadersNilEntityEmptySpec: Entity Headers=nil, spec Headers=[]string{}. Assert no headers update.

    If mocks are stale (MockK8sRepository missing PatchMutateStatusGlobalLoadBalancerConfig with correct signature), regenerate mocks via mockery.

    Tests must FAIL initially (RED) since the production code still has TODOs/commented-out calls.
  </action>
  <verify>
    <automated>go test ./internal/usecase/glbc_uc/... -run "TestDeployPool_PopulatesCreatedPoolMembers|TestDeployPool_StatusUpdatedOnCreate|TestBuildListenerUpdateRequest_Headers" -v -count=1</automated>
  </verify>
  <done>Test files exist and compile. Tests fail (RED) because production code does not yet have the implementation.</done>
</task>

<task type="auto">
  <name>Task 1: Activate STAT-01 pool member status tracking from spec on create path</name>
  <files>internal/usecase/glbc_uc/deploy_pool.go</files>
  <action>
    In deployPool(), replace the two commented-out TODO blocks on the create path (around lines 53-66) with:

    1. After CreateGlobalPool succeeds, build CreatedPoolMembers from spec data directly (NO ListGlobalPoolMembers call). For each poolMemberSpec in poolSpec.PoolMembers, create a CreatedGlobalPoolMember with Name from poolMemberSpec.Name (Id left empty — will be populated on subsequent reconcile via update path's deployPoolMembers). For each memberSpec in poolMemberSpec.Members, copy the GlobalMember directly into CreatedMembers.

    2. Call statusUpdatePoolMember(ctx, _pool.ID, poolSpec.Name, createdPoolMembers) BEFORE WaitGlobalLoadBalancerActive. If status save fails, return error.

    3. Then call WaitGlobalLoadBalancerActive.

    4. Return CreatedGlobalPool with CreatedPoolMembers populated.

    Key decisions (per user):
    - NO ListGlobalPoolMembers API call on create path (saves an API round-trip; member IDs will be populated on next reconcile)
    - CreatedGlobalPoolMember.Id left empty on create (only Name + CreatedMembers from spec)
    - Status save happens BEFORE WaitGlobalLoadBalancerActive (crash safety: if reconcile crashes mid-wait, status reflects what was created)
    - statusAddPool on update path (around line 92) remains commented out (deployPoolMembers already calls statusUpdatePoolMember)
  </action>
  <verify>
    <automated>go test ./internal/usecase/glbc_uc/... -run "TestDeployPool" -v -count=1</automated>
  </verify>
  <done>TestDeployPool_PopulatesCreatedPoolMembers passes (CreatedPoolMembers populated from spec, ListGlobalPoolMembers NOT called). TestDeployPool_StatusUpdatedOnCreate passes (PatchMutateStatusGlobalLoadBalancerConfig called).</done>
</task>

<task type="auto">
  <name>Task 2: Activate STAT-02 headers comparison + statusAddListener calls</name>
  <files>internal/usecase/glbc_uc/deploy_listener.go</files>
  <action>
    Two changes in deploy_listener.go:

    **A. Headers comparison in buildListenerUpdateRequest (STAT-02):**
    Replace the TODO comment (around line 214: "compare headers, somewhere is []string, somewhere is *string") with:

    1. Define local decodeHeaders func that takes *string, returns []string. If nil or empty string, return []string{}. Otherwise strings.Split on "," and lowercase each part.
    2. Decode currentListener.Headers into currentHeaders.
    3. Lowercase each entry in listenerSpec.Headers into specHeaders.
    4. Compare using stringSlicesEqualUnordered (already available in status.go, same package).
    5. If different: append headers change message, set updateOptions.Headers to joined specHeaders (or nil if empty), set isNeedUpdate = true.

    Key decisions (per user):
    - Case-insensitive comparison (both sides lowercased)
    - nil entity headers == empty spec headers (no spurious update)
    - Order-independent via stringSlicesEqualUnordered

    **B. Activate statusAddListener calls:**
    Uncomment the statusAddListener calls on both paths:
    1. Create path: statusAddListener(ctx, _lis.ID, int(listenerSpec.ProtocolPort)) BEFORE WaitGlobalLoadBalancerActive
    2. Update path: statusAddListener(ctx, currentListener.ID, currentListener.Port) BEFORE WaitGlobalLoadBalancerActive
  </action>
  <verify>
    <automated>go test ./internal/usecase/glbc_uc/... -run "TestBuildListenerUpdateRequest_Headers" -v -count=1</automated>
  </verify>
  <done>All three headers tests pass (change triggers update, no-change is silent, nil-vs-empty is silent). statusAddListener calls are active on both create and update paths, placed before WaitGlobalLoadBalancerActive. Full test suite (11 tests) passes with no regressions.</done>
</task>

</tasks>

<verification>
1. All 11 tests pass: `go test ./internal/usecase/glbc_uc/... -v -count=1`
2. Build succeeds: `go build ./internal/usecase/glbc_uc/...`
3. No remaining TODO comments for STAT-01 or STAT-02 in deploy_pool.go or deploy_listener.go (the intentional statusAddPool TODO on update path is acceptable)
</verification>

<success_criteria>
- STAT-01: deployPool create path populates CreatedPoolMembers from spec data, calls statusUpdatePoolMember before WaitGlobalLoadBalancerActive, and returns populated CreatedGlobalPool
- STAT-02: buildListenerUpdateRequest detects header drift via case-insensitive unordered comparison, triggering update only when headers actually differ
- statusAddListener is active on both create and update paths in deployListener, called before WaitGlobalLoadBalancerActive
- All 11 tests pass with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/02-status-and-validation-completeness/02-01-SUMMARY.md`
</output>
