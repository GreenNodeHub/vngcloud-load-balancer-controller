---
phase: 01-p0-bug-fixes
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/usecase/glbc_uc/deploy_pool_member.go
  - internal/usecase/glbc_uc/delete_lb.go
  - internal/usecase/glbc_uc/delete_pool.go
  - internal/usecase/glbc_uc/deploy_listener.go
  - internal/usecase/glbc_uc/deploy_pool_member_test.go
  - internal/usecase/glbc_uc/delete_lb_test.go
  - internal/usecase/glbc_uc/deploy_listener_test.go
autonomous: true
requirements:
  - BUG-02
  - BUG-03
  - BUG-04

must_haves:
  truths:
    - "convertMember includes SubnetID so pool member diffs are stable when SubnetID is unchanged"
    - "Empty GLB deletion calls DeleteGlobalLoadBalancer, not DeleteLoadBalancer"
    - "Empty pool deletion calls DeleteGlobalPool, not DeletePool"
    - "CreatedGlobalListener returned by deployListener has Name populated from API response"
  artifacts:
    - path: "internal/usecase/glbc_uc/deploy_pool_member.go"
      provides: "convertMember with SubnetID field"
      contains: "SubnetID"
    - path: "internal/usecase/glbc_uc/delete_lb.go"
      provides: "Correct GLB delete API call"
      contains: "DeleteGlobalLoadBalancer"
    - path: "internal/usecase/glbc_uc/delete_pool.go"
      provides: "Correct global pool delete API call"
      contains: "DeleteGlobalPool"
    - path: "internal/usecase/glbc_uc/deploy_listener.go"
      provides: "Listener name in status"
      contains: "Name: currentListener.Name"
    - path: "internal/usecase/glbc_uc/deploy_pool_member_test.go"
      provides: "Unit test for convertMember SubnetID"
    - path: "internal/usecase/glbc_uc/delete_lb_test.go"
      provides: "Unit test for DeleteGlobalLoadBalancer call"
    - path: "internal/usecase/glbc_uc/deploy_listener_test.go"
      provides: "Unit test for listener Name population"
  key_links:
    - from: "internal/usecase/glbc_uc/deploy_pool_member.go"
      to: "v1alpha1.GlobalMember"
      via: "convertMember return struct"
      pattern: "SubnetID.*member\\.SubnetID"
    - from: "internal/usecase/glbc_uc/delete_lb.go"
      to: "repository.VngCloudRepository"
      via: "DeleteGlobalLoadBalancer method call"
      pattern: "DeleteGlobalLoadBalancer"
---

<objective>
Fix three trivial P0 bugs (BUG-02, BUG-03, BUG-04) plus companion DeleteGlobalPool bug — all are 1-3 line targeted changes in the glbc_uc package.

Purpose: Unblock the reconciler's create/update/delete flows. These fixes are prerequisites for status stabilization and correct GLB lifecycle management.
Output: Four bug fixes with unit test coverage for each.
</objective>

<execution_context>
@/home/stackops/.claude/get-shit-done/workflows/execute-plan.md
@/home/stackops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-p0-bug-fixes/01-CONTEXT.md
@.planning/phases/01-p0-bug-fixes/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From internal/usecase/glbc_uc/deploy_pool_member.go:305-314 (BUG-02 target):
```go
func convertMember(member *entityv2.GlobalPoolMemberDetail) *v1alpha1.GlobalMember {
    return &v1alpha1.GlobalMember{
        Name:        member.Name,
        Address:     member.Address,
        Port:        member.Port,
        BackupRole:  member.BackupRole,
        Weight:      &member.Weight,
        MonitorPort: &member.MonitorPort,
        // SubnetID missing — causes spurious patches
    }
}
```

From internal/usecase/glbc_uc/delete_lb.go:64-66 (BUG-03 target):
```go
err = t.vngcloudRepo.DeleteLoadBalancer(ctx, lbId)
// Should be: t.vngcloudRepo.DeleteGlobalLoadBalancer(ctx, lbId)
```

From internal/usecase/glbc_uc/delete_pool.go:80 (companion bug):
```go
err := t.vngcloudRepo.DeletePool(ctx, lbId, candidateId)
// Should be: t.vngcloudRepo.DeleteGlobalPool(ctx, lbId, candidateId)
```

From internal/usecase/glbc_uc/deploy_listener.go:97-100 (BUG-04 target):
```go
return &v1alpha1.CreatedGlobalListener{
    Id:   currentListener.ID,
    Port: currentListener.Port,
    // Name missing
}, nil
```

From internal/repository/contracts.go (correct methods):
```go
DeleteGlobalLoadBalancer(ctx context.Context, glbID string) error
DeleteGlobalPool(ctx context.Context, glbID, poolID string) error
```

Test model: internal/usecase/lbc_uc/delete_listener_test.go — table-driven, MockVngCloudRepository, logrus.NewEntry(logrus.New()), &defaultModelDeployTask{...} direct construction.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for BUG-02, BUG-03, BUG-04</name>
  <files>internal/usecase/glbc_uc/deploy_pool_member_test.go, internal/usecase/glbc_uc/delete_lb_test.go, internal/usecase/glbc_uc/deploy_listener_test.go</files>
  <behavior>
    - TestConvertMember_IncludesSubnetID: Call convertMember with a GlobalPoolMemberDetail that has SubnetID="subnet-123". Assert returned GlobalMember.SubnetID == "subnet-123". (Will FAIL — convertMember currently omits SubnetID.)
    - TestDeleteLoadBalancer_CallsDeleteGlobalLoadBalancer: Set up a defaultModelDeployTask with mock repo. Mock isLoadBalancerEmpty to return true. Call deleteLoadBalancer path. Assert mock.DeleteGlobalLoadBalancer was called (not DeleteLoadBalancer). (Will FAIL — currently calls DeleteLoadBalancer.)
    - TestDeployListener_PopulatesName: Set up a deploy scenario where listener already exists (update path). Assert returned CreatedGlobalListener.Name is populated from the API entity. (Will FAIL — Name is currently empty.)
  </behavior>
  <action>
    Create three test files following the lbc_uc test pattern (table-driven, MockVngCloudRepository, logrus.NewEntry, direct struct construction).

    For deploy_pool_member_test.go:
    - Import entityv2 and v1alpha1 types
    - Create a GlobalPoolMemberDetail with all fields including SubnetID
    - Call convertMember, assert SubnetID is present in result

    For delete_lb_test.go:
    - Set up MockVngCloudRepository, mock ListGlobalListeners (empty), ListGlobalPools (empty) so isLoadBalancerEmpty returns true
    - Mock DeleteGlobalLoadBalancer to return nil
    - Construct defaultModelDeployTask with the mock and a GLBC status that has no created listeners/pools (full delete path)
    - Call the delete path, assert DeleteGlobalLoadBalancer was called via mock.AssertExpectations
    - Note: also test that DeleteGlobalPool is called instead of DeletePool — add a test where pools need deletion

    For deploy_listener_test.go:
    - Mock GetGlobalListener or ListGlobalListeners to return a listener with Name="test-listener"
    - Call deployListener, assert returned CreatedGlobalListener.Name == "test-listener"

    Run tests — all must FAIL (RED phase). Commit failing tests.
  </action>
  <verify>
    <automated>cd /home/stackops/vngcloud-load-balancer-controller-crd && go test ./internal/usecase/glbc_uc/... -v -run "TestConvertMember_IncludesSubnetID|TestDeleteLoadBalancer_CallsDeleteGlobalLoadBalancer|TestDeployListener_PopulatesName" 2>&1 | tail -20</automated>
  </verify>
  <done>Three test files exist with failing tests that verify each bug's expected behavior. Tests compile and run but FAIL on assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix BUG-02, BUG-03, BUG-04 + companion DeleteGlobalPool bug</name>
  <files>internal/usecase/glbc_uc/deploy_pool_member.go, internal/usecase/glbc_uc/delete_lb.go, internal/usecase/glbc_uc/delete_pool.go, internal/usecase/glbc_uc/deploy_listener.go</files>
  <behavior>
    - convertMember returns GlobalMember with SubnetID populated from input
    - deleteLoadBalancer empty-LB path calls DeleteGlobalLoadBalancer
    - deleteRedundantPools calls DeleteGlobalPool (not DeletePool)
    - deployListener returns CreatedGlobalListener with Name populated
  </behavior>
  <action>
    Apply four targeted fixes (GREEN phase):

    BUG-02 — deploy_pool_member.go:305-314:
    Add `SubnetID: member.SubnetID,` to the convertMember return struct.

    BUG-03 — delete_lb.go:66:
    Change `t.vngcloudRepo.DeleteLoadBalancer(ctx, lbId)` to `t.vngcloudRepo.DeleteGlobalLoadBalancer(ctx, lbId)`.

    Companion bug — delete_pool.go:80:
    Change `t.vngcloudRepo.DeletePool(ctx, lbId, candidateId)` to `t.vngcloudRepo.DeleteGlobalPool(ctx, lbId, candidateId)`.

    BUG-04 — deploy_listener.go:97-100:
    Add `Name: currentListener.Name,` to the CreatedGlobalListener return struct.

    Run all tests — must PASS (GREEN phase). Then run full glbc_uc test suite to verify no regressions. Commit fixes.
  </action>
  <verify>
    <automated>cd /home/stackops/vngcloud-load-balancer-controller-crd && go test ./internal/usecase/glbc_uc/... -v 2>&1 | tail -30</automated>
  </verify>
  <done>All four bugs fixed. Tests pass. No regressions in glbc_uc test suite. convertMember includes SubnetID, delete calls use correct Global* methods, listener name is populated.</done>
</task>

</tasks>

<verification>
- `go test ./internal/usecase/glbc_uc/... -v` — all tests pass
- `go vet ./internal/usecase/glbc_uc/...` — no issues
- Grep confirms: no remaining `DeleteLoadBalancer` calls in glbc_uc (only `DeleteGlobalLoadBalancer`), no remaining `DeletePool` calls (only `DeleteGlobalPool`), `SubnetID` present in convertMember, `Name` present in deployListener return
</verification>

<success_criteria>
- convertMember output includes SubnetID field
- delete_lb.go calls DeleteGlobalLoadBalancer (not DeleteLoadBalancer) when LB is empty
- delete_pool.go calls DeleteGlobalPool (not DeletePool) when deleting pools
- deployListener returns CreatedGlobalListener with Name populated
- Unit tests verify each fix
- Full glbc_uc test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/01-p0-bug-fixes/01-01-SUMMARY.md`
</output>
