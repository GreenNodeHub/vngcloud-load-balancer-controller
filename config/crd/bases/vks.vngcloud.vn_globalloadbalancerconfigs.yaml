---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: globalloadbalancerconfigs.vks.vngcloud.vn
spec:
  group: vks.vngcloud.vn
  names:
    kind: GlobalLoadBalancerConfig
    listKind: GlobalLoadBalancerConfigList
    plural: globalloadbalancerconfigs
    shortNames:
    - glbc
    singular: globalloadbalancerconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GlobalLoadBalancerConfig is the Schema for the globalloadbalancerconfigs
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of GlobalLoadBalancerConfig
            properties:
              description:
                description: Description is the description of the load balancer
                type: string
              globalListeners:
                description: GlobalListeners defines the array of listeners for the
                  load balancer
                items:
                  properties:
                    allowedCidrs:
                      description: AllowedCidrs defines the allowed CIDR blocks
                      type: string
                    defaultPoolName:
                      description: DefaultPoolName is the name of the default pool
                      type: string
                    description:
                      description: Description is an optional description for the
                        listener
                      type: string
                    headers:
                      description: Headers defines headers to insert into requests
                      items:
                        type: string
                      type: array
                    name:
                      description: Name is the name of the listener
                      type: string
                    protocol:
                      description: Protocol is the protocol for the listener
                      type: string
                    protocolPort:
                      description: ProtocolPort is the port number for the listener
                      maximum: 65535
                      minimum: 1
                      type: integer
                    timeoutClient:
                      description: TimeoutClient is the client timeout in seconds
                      format: int32
                      type: integer
                    timeoutConnection:
                      description: TimeoutConnection is the connection timeout in
                        seconds
                      format: int32
                      type: integer
                    timeoutMember:
                      description: TimeoutMember is the member timeout in seconds
                      format: int32
                      type: integer
                  required:
                  - name
                  - protocol
                  - protocolPort
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              globalPools:
                description: GlobalPools defines the array of pools for the load balancer
                items:
                  properties:
                    algorithm:
                      description: Algorithm is the load balancing algorithm for the
                        pool
                      type: string
                    description:
                      description: Description is an optional description for the
                        pool
                      type: string
                    healthMonitor:
                      description: HealthMonitor defines the health monitor configuration
                        for the pool
                      properties:
                        domainName:
                          description: DomainName is the hostname sent in the HTTP
                            Host header
                          type: string
                        healthCheckMethod:
                          description: HealthCheckMethod specifies how the health
                            check request is made (e.g., GET, TCP)
                          type: string
                        healthCheckPath:
                          description: HealthCheckPath is the path used for HTTP health
                            checks
                          type: string
                        healthyThreshold:
                          description: HealthyThreshold is the number of consecutive
                            successful checks before marking healthy
                          type: integer
                        httpVersion:
                          description: HttpVersion defines which HTTP version to use
                            for HTTP-based health checks
                          type: string
                        interval:
                          description: Interval is the time in seconds between each
                            health check
                          type: integer
                        protocol:
                          description: Protocol is the protocol used for health checks
                          type: string
                        successCode:
                          description: SuccessCode specifies which HTTP codes indicate
                            a healthy response
                          type: string
                        timeout:
                          description: Timeout is the maximum time in seconds to wait
                            for a response
                          type: integer
                        unhealthyThreshold:
                          description: UnhealthyThreshold is the number of consecutive
                            failed checks before marking unhealthy
                          type: integer
                      required:
                      - protocol
                      type: object
                    name:
                      description: Name is the name of the pool
                      type: string
                    poolMembers:
                      description: PoolMembers is the list of members in the pool
                      items:
                        properties:
                          description:
                            description: Description is an optional description for
                              the pool member
                            type: string
                          members:
                            description: Members is the list of member
                            items:
                              properties:
                                address:
                                  description: Address is the IP address of the member
                                  type: string
                                backupRole:
                                  description: BackupRole indicates if the member
                                    is a backup
                                  type: boolean
                                description:
                                  description: Description is an optional description
                                    for the member
                                  type: string
                                monitorPort:
                                  description: MonitorPort is the port used for health
                                    monitoring
                                  type: integer
                                name:
                                  description: Name is the name of the member
                                  type: string
                                port:
                                  description: Port is the port on which the member
                                    receives traffic
                                  type: integer
                                subnetId:
                                  description: SubnetID is the ID of the subnet where
                                    the member resides
                                  type: string
                                weight:
                                  description: Weight is the weight of the member
                                    for load balancing
                                  type: integer
                              required:
                              - address
                              - name
                              - port
                              - subnetId
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          name:
                            description: Name is an optional name for the pool member
                            type: string
                          region:
                            description: Region is the region where the pool member
                              is located
                            type: string
                          trafficDial:
                            description: TrafficDial is the traffic dial percentage
                              for the pool member
                            type: integer
                          type:
                            description: Type is the type of the pool member
                            type: string
                          vpcId:
                            description: VpcId is the ID of the VPC where the pool
                              member resides
                            type: string
                        required:
                        - region
                        - type
                        - vpcId
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                    protocol:
                      description: Protocol is the protocol for the pool
                      type: string
                    stickiness:
                      description: Stickiness enables sticky sessions for the pool
                      type: boolean
                    tlsEncryption:
                      description: TLSEncryption enables TLS encryption for the pool
                      type: boolean
                  required:
                  - name
                  - protocol
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              loadBalancerId:
                description: LoadBalancerId is managed by the controller
                type: string
              name:
                description: |-
                  General fields for all load balancers
                  Name is the name of the load balancer
                type: string
              packageId:
                description: PackageId is the size/package of the load balancer
                type: string
              paymentFlow:
                description: PaymentFlow defines the payment flow for the load balancer
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Tags are key-value pairs for load balancer tagging
                type: object
              type:
                description: Type defines the type of global load balancer
                type: string
            required:
            - name
            - type
            type: object
          status:
            description: status defines the observed state of GlobalLoadBalancerConfig
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the GlobalLoadBalancerConfig resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              createdListeners:
                description: CreatedListeners is the list of created listener IDs
                items:
                  properties:
                    id:
                      description: Id is the ID of the created listener
                      type: string
                    name:
                      description: Name is the name of the listener
                      type: string
                    port:
                      description: Port is the port number of the created listener
                      type: integer
                  required:
                  - id
                  - name
                  - port
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              createdPools:
                description: CreatedPools is the list of created pool IDs
                items:
                  properties:
                    createdPoolMembers:
                      description: CreatedPoolMembers is the list of created member
                        IDs
                      items:
                        properties:
                          createdMembers:
                            description: CreatedMembers is the list of created members
                            items:
                              properties:
                                address:
                                  description: Address is the IP address of the member
                                  type: string
                                backupRole:
                                  description: BackupRole indicates if the member
                                    is a backup
                                  type: boolean
                                description:
                                  description: Description is an optional description
                                    for the member
                                  type: string
                                monitorPort:
                                  description: MonitorPort is the port used for health
                                    monitoring
                                  type: integer
                                name:
                                  description: Name is the name of the member
                                  type: string
                                port:
                                  description: Port is the port on which the member
                                    receives traffic
                                  type: integer
                                subnetId:
                                  description: SubnetID is the ID of the subnet where
                                    the member resides
                                  type: string
                                weight:
                                  description: Weight is the weight of the member
                                    for load balancing
                                  type: integer
                              required:
                              - address
                              - name
                              - port
                              - subnetId
                              type: object
                            type: array
                          id:
                            description: Id is the ID of the created member
                            type: string
                          name:
                            description: Name is the name of the created member
                            type: string
                        required:
                        - id
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                    id:
                      description: Id is the ID of the created pool
                      type: string
                    name:
                      description: Name is the name of the created pool
                      type: string
                  required:
                  - id
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              createdTags:
                additionalProperties:
                  type: string
                description: CreatedTags is the map of tags created on the load balancer
                type: object
              domains:
                description: Domains is the list of DNS hostnames assigned to the
                  load balancer
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              lastReconcileMessage:
                description: LastReconcileMessage contains a message from the last
                  reconciliation
                type: string
              lastReconcileTime:
                description: LastReconcileTime is the timestamp of the last reconciliation
                  attempt
                format: date-time
                type: string
              loadBalancerId:
                description: LoadBalancerId is the actual ID of the load balancer
                  in VNG Cloud
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed spec
                format: int64
                type: integer
              vips:
                description: Vips is the list of Virtual IPs assigned to the load
                  balancer across different regions
                items:
                  description: GlobalLoadBalancerVIPStatus represents a Virtual IP
                    assigned to the global load balancer
                  properties:
                    address:
                      description: Address is the IP address of the VIP
                      type: string
                    region:
                      description: Region is the region where the VIP is located
                      type: string
                    status:
                      description: Status is the status of the VIP (e.g., "ACTIVE",
                        "PENDING")
                      type: string
                  required:
                  - address
                  - region
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - region
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
