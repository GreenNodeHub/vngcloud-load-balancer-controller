---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: vngcloudloadbalancerconfigs.vks.vngcloud.vn
spec:
  group: vks.vngcloud.vn
  names:
    kind: VngcloudLoadBalancerConfig
    listKind: VngcloudLoadBalancerConfigList
    plural: vngcloudloadbalancerconfigs
    singular: vngcloudloadbalancerconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VngcloudLoadBalancerConfig is the Schema for the vngcloudloadbalancerconfigs
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of VngcloudLoadBalancerConfig
            properties:
              application:
                description: |-
                  Application contains configuration specific to application (L7) load balancers
                  This field is only valid when Type is "application"
                properties:
                  autoReorderPolicies:
                    description: AutoReorderPolicies enables automatic reordering
                      of policies
                    type: boolean
                  certificateIDs:
                    description: CertificateIDs are the SSL certificate IDs for HTTPS
                      listeners
                    items:
                      type: string
                    type: array
                  clientCertificateID:
                    description: ClientCertificateID is the client certificate ID
                      for mutual TLS
                    type: string
                  enableStickySession:
                    description: EnableStickySession enables sticky sessions for application
                      load balancers
                    type: boolean
                  enableTLSEncryption:
                    description: EnableTLSEncryption enables TLS encryption for application
                      load balancers
                    type: boolean
                  header:
                    additionalProperties:
                      type: string
                    description: Header configuration for application load balancers
                    type: object
                  implementationSpecificParams:
                    additionalProperties:
                      type: string
                    description: ImplementationSpecificParams are implementation-specific
                      parameters
                    type: object
                  insertHeaders:
                    additionalProperties:
                      type: string
                    description: InsertHeaders are headers to insert into requests
                    type: object
                type: object
              attachSecurityGroupsToNodes:
                description: AttachSecurityGroupsToNodes contains security groups
                  to attach to nodes (filter by TargetNodeLabels)
                items:
                  type: string
                type: array
              autoManageSecurityGroupRules:
                description: AutoManageSecurityGroupRules defines security group rules
                  to be auto-managed by the controller attached to nodes (filter by
                  TargetNodeLabels)
                items:
                  properties:
                    cidr:
                      description: CIDR is the CIDR block for the rule
                      type: string
                    description:
                      description: Description is an optional description for the
                        rule
                      type: string
                    fromPort:
                      description: FromPort is the starting port for the rule
                      format: int32
                      type: integer
                    protocol:
                      description: Protocol is the protocol for the security group
                        rule
                      type: string
                    toPort:
                      description: ToPort is the ending port for the rule
                      format: int32
                      type: integer
                  required:
                  - cidr
                  - fromPort
                  - protocol
                  - toPort
                  type: object
                type: array
              enableAutoscale:
                description: EnableAutoscale enables autoscaling for the load balancer
                type: boolean
              idleTimeoutClient:
                description: IdleTimeoutClient is the idle timeout for client connections
                format: int32
                type: integer
              idleTimeoutConnection:
                description: IdleTimeoutConnection is the idle timeout for connections
                format: int32
                type: integer
              idleTimeoutMember:
                description: IdleTimeoutMember is the idle timeout for member connections
                format: int32
                type: integer
              inboundCIDRs:
                description: InboundCIDRs defines the inbound CIDR blocks
                items:
                  type: string
                type: array
              isPoc:
                description: IsPOC indicates if this is a proof of concept deployment
                type: boolean
              listeners:
                description: Listeners defines the array of listeners for the load
                  balancer
                items:
                  description: Listener defines a listener configuration for the load
                    balancer
                  properties:
                    allowedCidrs:
                      description: AllowedCidrs defines the allowed CIDR blocks
                      type: string
                    certificateAuthorities:
                      description: CertificateAuthorities is a list of certificate
                        authority IDs
                      items:
                        type: string
                      type: array
                    clientCertificateAuthentication:
                      description: ClientCertificateAuthentication defines client
                        certificate authentication settings
                      type: string
                    defaultCertificateAuthority:
                      description: DefaultCertificateAuthority is the default certificate
                        authority
                      type: string
                    defaultPoolName:
                      description: DefaultPoolName is the name of the default pool
                      type: string
                    description:
                      description: Description is an optional description for the
                        listener
                      type: string
                    headers:
                      description: Headers is a list of headers to forward
                      items:
                        type: string
                      type: array
                    insertHeaders:
                      description: InsertHeaders defines headers to insert into requests
                      items:
                        description: InsertHeader defines a header to be inserted
                          in requests
                        properties:
                          headerName:
                            description: HeaderName is the name of the header to insert
                            type: string
                          headerValue:
                            description: HeaderValue is the value of the header to
                              insert
                            type: string
                        required:
                        - headerName
                        - headerValue
                        type: object
                      type: array
                    name:
                      description: Name is the name of the listener
                      type: string
                    policies:
                      description: Policies is the list of policies for this listener
                        (for application load balancers)
                      items:
                        description: Policy defines a policy configuration for application
                          load balancer listeners
                        properties:
                          action:
                            description: Action defines the action to take
                            enum:
                            - REDIRECT_TO_POOL
                            - REDIRECT_TO_URL
                            - REJECT
                            type: string
                          description:
                            description: Description is an optional description for
                              the policy
                            type: string
                          keepQueryString:
                            description: KeepQueryString determines if query string
                              should be kept on redirect
                            type: boolean
                          l7Rules:
                            description: L7Rules is the list of L7 rules for this
                              policy
                            items:
                              description: L7Rule defines a Layer 7 rule for policies
                              properties:
                                compareType:
                                  description: CompareType is how to compare the rule
                                    value
                                  enum:
                                  - CONTAINS
                                  - STARTS_WITH
                                  - ENDS_WITH
                                  - REGEX
                                  - EQUAL_TO
                                  type: string
                                ruleType:
                                  description: RuleType is the type of rule
                                  enum:
                                  - HOST_NAME
                                  - PATH
                                  type: string
                                ruleValue:
                                  description: RuleValue is the value to compare against
                                  type: string
                              required:
                              - compareType
                              - ruleType
                              - ruleValue
                              type: object
                            type: array
                          name:
                            description: Name is the name of the policy
                            type: string
                          position:
                            description: Position is the position/priority of the
                              policy
                            format: int32
                            minimum: 1
                            type: integer
                          redirectHttpCode:
                            description: RedirectHttpCode is the HTTP code to use
                              for redirect
                            enum:
                            - 301
                            - 302
                            - 303
                            - 307
                            - 308
                            format: int32
                            type: integer
                          redirectPoolName:
                            description: RedirectPoolName is the name of the pool
                              to redirect to
                            type: string
                          redirectUrl:
                            description: RedirectUrl is the URL to redirect to (for
                              REDIRECT_TO_URL action)
                            type: string
                        required:
                        - action
                        - name
                        type: object
                      type: array
                    protocol:
                      description: Protocol is the protocol for the listener
                      enum:
                      - TCP
                      - UDP
                      - HTTP
                      - HTTPS
                      type: string
                    protocolPort:
                      description: ProtocolPort is the port number for the listener
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    timeoutClient:
                      description: TimeoutClient is the client timeout in seconds
                      format: int32
                      type: integer
                    timeoutConnection:
                      description: TimeoutConnection is the connection timeout in
                        seconds
                      format: int32
                      type: integer
                    timeoutMember:
                      description: TimeoutMember is the member timeout in seconds
                      format: int32
                      type: integer
                  required:
                  - name
                  - protocol
                  - protocolPort
                  type: object
                type: array
              loadBalancerID:
                description: LoadBalancerID is managed by the controller
                type: string
              loadBalancerName:
                description: |-
                  General fields for all load balancers
                  LoadBalancerName is the name of the load balancer (only set via annotation)
                type: string
              network:
                description: |-
                  Network contains configuration specific to network (L4) load balancers
                  This field is only valid when Type is "network"
                properties:
                  enableProxyProtocol:
                    description: EnableProxyProtocol enables proxy protocol for network
                      load balancers
                    type: boolean
                type: object
              packageID:
                description: PackageID is the size/package of the load balancer
                type: string
              poolAlgorithm:
                description: PoolAlgorithm is the load balancing algorithm
                enum:
                - ROUND_ROBIN
                - LEAST_CONNECTIONS
                - SOURCE_IP
                type: string
              pools:
                description: Pools defines the array of pools for the load balancer
                items:
                  description: Pool defines a pool configuration for the load balancer
                  properties:
                    algorithm:
                      description: Algorithm is the load balancing algorithm for the
                        pool
                      enum:
                      - ROUND_ROBIN
                      - LEAST_CONNECTIONS
                      - SOURCE_IP
                      type: string
                    description:
                      description: Description is an optional description for the
                        pool
                      type: string
                    healthMonitor:
                      description: HealthMonitor defines the health monitor configuration
                        for the pool
                      properties:
                        domainName:
                          description: DomainName is the hostname sent in the HTTP
                            Host header
                          type: string
                        healthCheckMethod:
                          description: HealthCheckMethod specifies how the health
                            check request is made (e.g., GET, TCP)
                          type: string
                        healthCheckPath:
                          description: HealthCheckPath is the path used for HTTP health
                            checks
                          type: string
                        healthyThreshold:
                          description: HealthyThreshold is the number of consecutive
                            successful checks before marking healthy
                          type: integer
                        httpVersion:
                          description: HttpVersion defines which HTTP version to use
                            for HTTP-based health checks
                          type: string
                        interval:
                          description: Interval is the time in seconds between each
                            health check
                          type: integer
                        protocol:
                          description: Protocol is the protocol used for health checks
                          type: string
                        successCode:
                          description: SuccessCode specifies which HTTP codes indicate
                            a healthy response
                          type: string
                        timeout:
                          description: Timeout is the maximum time in seconds to wait
                            for a response
                          type: integer
                        unhealthyThreshold:
                          description: UnhealthyThreshold is the number of consecutive
                            failed checks before marking unhealthy
                          type: integer
                      required:
                      - protocol
                      type: object
                    members:
                      description: Members is the list of members in the pool
                      items:
                        description: PoolMember defines a member of a load balancer
                          pool
                        properties:
                          backup:
                            description: Backup indicates if the member is a backup
                              member
                            type: boolean
                          ip:
                            description: IP is the IP address of the pool member
                            type: string
                          monitorPort:
                            description: MonitorPort is the monitor port of the pool
                              member
                            type: integer
                          name:
                            description: Name is an optional name for the pool member
                            type: string
                          port:
                            description: Port is the port number of the pool member
                            type: integer
                          weight:
                            description: Weight is the weight of the pool member
                            type: integer
                        required:
                        - ip
                        - monitorPort
                        - port
                        type: object
                      type: array
                    name:
                      description: Name is the name of the pool
                      type: string
                    protocol:
                      description: Protocol is the protocol for the pool
                      enum:
                      - TCP
                      - UDP
                      - HTTP
                      - PROXY
                      type: string
                    stickiness:
                      description: Stickiness enables sticky sessions for the pool
                      type: boolean
                    tlsEncryption:
                      description: TLSEncryption enables TLS encryption for the pool
                      type: boolean
                  required:
                  - name
                  - protocol
                  type: object
                type: array
              preferZoneID:
                description: PreferZoneID is the preferred zone ID for placement
                type: string
              scheme:
                description: Scheme defines if the load balancer is internal or external
                enum:
                - Internal
                - Internet
                type: string
              securityGroups:
                description: SecurityGroups are the security groups for the load balancer
                items:
                  type: string
                type: array
              subnetID:
                description: SubnetID is the subnet ID for the load balancer
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Tags are key-value pairs for load balancer tagging
                type: object
              targetNodeLabels:
                additionalProperties:
                  type: string
                description: TargetNodeLabels are labels for targeting specific nodes
                type: object
              type:
                description: Type defines the type of load balancer (network or application)
                enum:
                - Layer 4
                - Layer 7
                type: string
            required:
            - loadBalancerName
            - subnetID
            - type
            type: object
          status:
            description: status defines the observed state of VngcloudLoadBalancerConfig
            properties:
              address:
                description: Address is the DNS name or IP address assigned to the
                  load balancer
                type: string
              loadBalancerID:
                description: LoadBalancerID is the actual ID of the load balancer
                  in VNG Cloud
                type: string
              manageDFPMembers:
                description: ManageDFPMembers indicates if the controller should manage
                  DFP members
                type: boolean
              manageListeners:
                description: ManageListeners indicates if the controller should manage
                  listeners
                type: boolean
              managePools:
                description: ManagePools indicates if the controller should manage
                  pools
                type: boolean
              message:
                description: Message provides human-readable details about the current
                  state
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed spec
                format: int64
                type: integer
              reason:
                description: Reason provides a brief reason for the current state
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Tags are key-value pairs for load balancer tagging
                type: object
              updatedAt:
                description: UpdatedAt is the timestamp when the load balancer was
                  last updated
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
