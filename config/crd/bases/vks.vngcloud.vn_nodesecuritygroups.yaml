---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: nodesecuritygroups.vks.vngcloud.vn
spec:
  group: vks.vngcloud.vn
  names:
    kind: NodeSecurityGroup
    listKind: NodeSecurityGroupList
    plural: nodesecuritygroups
    shortNames:
    - nsg
    singular: nodesecuritygroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NodeSecurityGroup is the Schema for the nodesecuritygroups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NodeSecurityGroup
            properties:
              attachSecurityGroups:
                description: AttachSecurityGroups is a list of existing security group
                  IDs to attach to the nodes
                items:
                  type: string
                type: array
              managedSecurityGroup:
                description: ManagedSecurityGroup defines a security group to be created
                  and managed
                properties:
                  description:
                    description: Description of the security group
                    type: string
                  name:
                    description: Name is the name of the security group
                    type: string
                  rules:
                    description: Rules is the list of security group rules
                    items:
                      description: NodeSecurityGroupRule defines a security group
                        rule for NodeSecurityGroup
                      properties:
                        cidr:
                          description: CIDR is the CIDR block for the rule
                          type: string
                        description:
                          description: Description is an optional description for
                            the rule
                          type: string
                        direction:
                          description: Direction is the direction of the rule (ingress
                            or egress)
                          type: string
                        etherType:
                          description: EtherType is the ethertype of the rule (IPv4
                            or IPv6)
                          type: string
                        fromPort:
                          description: FromPort is the starting port for the rule
                          format: int32
                          type: integer
                        protocol:
                          description: Protocol is the protocol for the security group
                            rule
                          type: string
                        toPort:
                          description: ToPort is the ending port for the rule
                          format: int32
                          type: integer
                      required:
                      - cidr
                      - fromPort
                      - protocol
                      - toPort
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                required:
                - name
                type: object
              selectNodeLabels:
                additionalProperties:
                  type: string
                description: SelectNodeLabels is a map of labels to select nodes
                type: object
            type: object
          status:
            description: status defines the observed state of NodeSecurityGroup
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the NodeSecurityGroup resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastReconcileMessage:
                description: LastReconcileMessage contains a message from the last
                  reconciliation
                type: string
              lastReconcileTime:
                description: LastReconcileTime is the timestamp of the last reconciliation
                  attempt
                format: date-time
                type: string
              managedSecurityGroup:
                description: ManagedSecurityGroup contains the status of the managed
                  security group
                properties:
                  error:
                    description: Error contains the error message when ensuring the
                      managed security group
                    type: string
                  id:
                    description: Id is the id of the managed security group
                    type: string
                type: object
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the controller
                format: int64
                type: integer
              selectedNodes:
                description: SelectedNodes is the list of nodes that match the node
                  selector
                items:
                  description: NodeInfo contains information about a selected node
                  properties:
                    name:
                      description: Name is the name of the node
                      type: string
                    serverId:
                      description: ServerId is the VNG Cloud server ID of the node
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              serverSecurityGroups:
                description: ServerSecurityGroups tracks the security group attachment
                  status for each server
                items:
                  description: ServerSecurityGroupStatus tracks the security group
                    attachment status for a specific server
                  properties:
                    attachedSecurityGroupIds:
                      description: AttachedSecurityGroupIds is the list of security
                        group IDs successfully attached to this server
                      items:
                        type: string
                      type: array
                    error:
                      description: Error contains the error message if attachment
                        failed
                      type: string
                    serverId:
                      description: ServerId is the VNG Cloud server ID
                      type: string
                  required:
                  - serverId
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - serverId
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
