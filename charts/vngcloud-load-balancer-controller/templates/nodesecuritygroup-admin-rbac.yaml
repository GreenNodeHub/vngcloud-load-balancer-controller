apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "vngcloud-load-balancer-controller.fullname" . }}-nodesecuritygroup-admin-role
  labels:
  {{- include "vngcloud-load-balancer-controller.labels" . | nindent 4 }}
rules:
- apiGroups:
  - vks.vngcloud.vn
  resources:
  - nodesecuritygroups
  verbs:
  - '*'
- apiGroups:
  - vks.vngcloud.vn
  resources:
  - nodesecuritygroups/status
  verbs:
  - get
